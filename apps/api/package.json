{
  "name": "@music-explorer/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "bun run src/dev.ts",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@libsql/client": "^0.17.0",
    "@music-explorer/db": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.4",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "vitest": "^3.2.3",
    "wrangler": "^4.15.2"
  }
}
